apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: homelab-gateway
spec:
  selector:
    matchLabels:
      app: homelab-gateway
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
